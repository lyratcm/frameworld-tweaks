@advanced_cost = 600
@advanced_rural_cost = 600
@advanced_rare_cost = 30
@advanced_upkeep = 3
@advanced_rare_upkeep = 1
@base_time = 240
@city_time = 480
@base_housing = 100
@yes = 1
@no = 0


inline_script = {
	script = planet/minerals/fwt_miner_job_swap
	jobs = @frameworld_jobs_base
	district = 1
	extra_hive_jobs = @[ 100 * $SIZE_COST$ ]
	multiplier_string = "mult = fwt_mineral_completion"
}
base_buildtime = @[ city_time * $SIZE_COST$ * $SCALE$ ]

resources = {
	category = planet_districts
	cost = {
		minerals = @[ advanced_rural_cost * $SIZE_COST$ * $SCALE$ ]
		mult = fwt_mineral_completion
	}
	#half advanced cost (so base) and 0x advanced so none of the for tier 1
	upkeep = {
		energy = @[ advanced_upkeep * $SIZE_COST$ * $SCALE$ ]
		mult = fwt_mineral_completion
	}
	cost = {
		trigger = {
			check_variable = { which = fwt_mineral_completion value > 1 }
		}
		rare_crystals = @[ advanced_rare_cost * $SIZE$ * $SCALE$ ]
		mult = fwt_mineral_completion
	}
	#half advanced cost (so base) and 0x advanced so none of the for tier 1
	upkeep = {
		trigger = {
			check_variable = { which = fwt_mineral_completion value > 1 }
		}
		rare_crystals = @[ advanced_rare_upkeep * $SIZE$ * $SCALE$ ]
		mult = fwt_mineral_completion
	}
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_valid_civic = civic_agrarian_idyll
		}
	}
	modifier = {
		planet_housing_add = 50
	}
}

planet_modifier = {
	zone_building_slots_add = 3
}

inline_script = {
	script = zones/shared_frameworld_non_urban_zone_modifiers
}